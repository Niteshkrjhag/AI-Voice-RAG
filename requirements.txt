# ============================================================
# Production Voice AI + RAG — Dependencies
# ============================================================

# --- Core Framework ---
fastapi>=0.115.0                # REST API for KB retrieval endpoint
uvicorn[standard]>=0.34.0       # ASGI server for FastAPI
python-dotenv>=1.1.0            # Environment variable management
pydantic>=2.11.0                # Data validation and schema enforcement

# --- Q2: Knowledge Base Pipeline ---
crawl4ai>=0.9.0                 # Web scraping + extraction (open source, v0.9.x)
sentence-transformers>=3.0.0    # Local vector embeddings
torch>=2.0.0                    # Backend for sentence-transformers
qdrant-client>=1.18.0           # Vector database client (latest 2026)
beautifulsoup4>=4.13.0          # HTML parsing for cleanup
langchain-text-splitters>=0.3.8 # Semantic text chunking
tiktoken>=0.9.0                 # Token counting for chunking

# --- Q1 + Q3: Voice Agent (Vapi) ---
requests>=2.32.0                # HTTP requests for Vapi REST API + webhooks
google-genai>=2.14.0            # Google Gemini SDK (unified client pattern)

# --- Q4: Real-Time Insights Pipeline ---
assemblyai>=0.37.0              # Streaming ASR v3 (StreamingClient, TurnEvent)
websockets>=15.0                # WebSocket server for nudge delivery
websocket-client>=1.8.0         # WebSocket client for raw WS connections
jinja2>=3.1.0                   # Dashboard templating

# --- Shared Utilities ---
structlog>=25.4.0               # Structured logging
aiofiles>=24.1.0                # Async file operations
httpx>=0.28.0                   # Async HTTP client

